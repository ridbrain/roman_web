import 'package:flutter/material.dart';

const String apiUrl = "https://server.roman.com.ru/web/scripts/";

const BorderRadius radius = BorderRadius.all(
  Radius.circular(8),
);

const List<BoxShadow> shadow = [
  BoxShadow(
    color: Colors.grey,
    spreadRadius: 1,
    blurRadius: 7,
  ),
];

const tableHeadStyle = TextStyle(
  fontWeight: FontWeight.bold,
  fontSize: 15,
);

const Color colorGrey = Color(0xFF212121);
